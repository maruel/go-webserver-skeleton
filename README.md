# Skeleton for a basic HTTP server

Try out:

```
curl -d '{}' http://localhost:8081/api/log
curl -d '{"Name":"stdout"}' http://localhost:8081/api/log
curl -d '{"Name":"stderr"}' http://localhost:8081/api/log
curl http://localhost:8081/quitquitquit
```
